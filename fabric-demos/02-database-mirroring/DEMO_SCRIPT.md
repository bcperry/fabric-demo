# Demo 02 Script

1. Open the external PostgreSQL database and show one simulated readiness row.
2. Open `MDA Operations Mirror` and show the selected tables in Running state.
3. Run `01_validate_mirror` and compare source-contract counts with mirrored counts.
4. Update one approved demo row in PostgreSQL and show it arriving without a file export.
5. Open lineage and show that Stage 03 consumes the mirror.
6. Close with the leadership value: operational state is current and traceable.

Expected time: 12 minutes.
